mongodb {
  uri = "mongodb://root:pass@localhost:27017/diet?authSource=admin"
  mealCollection = "meal"
  productCollection = "product"
}

mealDomain {
  url = "jdbc:postgresql://localhost:5434/chef"
  user = "dev"
  password = "secret"
  mealTable = "meal"
  productTable = "product"
  mealProductTable = "meal_product"
}

productDomain {
  url = "jdbc:postgresql://localhost:5432/food"
  user = "dev"
  password = "secret"
  productTable = "product"
}