mongodb {
  uri = "mongodb://root:pass@localhost:27017/diet?authSource=admin"
  database = "diet"
  mealCollection = "meal"
}

mealDomain {
  url = "jdbc:postgresql://localhost:5432/chef"
  user = "dev"
  password = "secret"
  mealTable = "meal"
  productTable = "product"
  mealProductTable = "meal_product"
}

productDomain {
  url = "jdbc:postgresql://localhost:5432/food"
  user = "dev"
  password = "secret"
  productTable = "product"
}